#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);
  
  // Copy all input to output as is
  string line;
  while (getline(cin, line)) {
      cout << line << "\n";
  }

}